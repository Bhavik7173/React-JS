JavaScript
    client-side scripting language
    use for the validation

    to run the HTML programs : browser (client)

JavaScript will be execute on browser
---------------------------------------------------------
How to integrate HTML and JavaScript?
 - internal JS
 - external JS

    HTML + JS : file.html ==> browser
    for the JS code, need to use following tag
        <script> js code </script>
    in HTML there are mainly two tags : HEAD,BODY
        HTML => HEAD + BODY
        we can put our <script> tag in either of tags (HEAD,BODY)

---------------------------------------------------------
output
    1) console.log() : to display the msg on the log
    2) document.write(msg) : to display msg on the browser
    3) alert() : create a dailog box
    4) HTML-ELEMENT.innerHTML = value : to set data to HTML-ELEMENT
---------------------------------------------------------
DOM : document object modeling    
---------------------------------------------------------
Input
    1) promt
    2) from HTML element
---------------------------------------------------------